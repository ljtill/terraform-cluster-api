---
apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: ${westeurope:name}
  namespace: capi-workload-clusters
spec:
  controlPlaneRef:
    apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
    kind: AzureManagedControlPlane
    name: ${westeurope:name}
    namespace: capi-workload-clusters
  infrastructureRef:
    apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
    kind: AzureManagedCluster
    name: ${westeurope:name}
    namespace: capi-workload-clusters
---
apiVersion: cluster.x-k8s.io/v1beta1
kind: MachinePool
metadata:
  name: ${westeurope:name}
  namespace: capi-workload-clusters
spec:
  clusterName: ${westeurope:name}
  replicas: 3
  template:
    spec:
      bootstrap:
        dataSecretName: ""
      clusterName: ${westeurope:name}
      infrastructureRef:
        apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
        kind: AzureManagedMachinePool
        name: ${westeurope:name}
        namespace: capi-workload-clusters
      version: v1.24.9
